// RUN: b3 resolve %s | OutputCheck %s
// CHECK: closure elaboration not yet fully implemented

// Test that closure elaboration returns appropriate error
function F(): int

procedure TestClosureElaboration()
{
  var x := lift
    %val := 42
  into
    %r: int
  by {
    %r == F()
  }
}
